import React from 'react'
import {Typography} from '@material-ui/core'

const App = props => {
  return <div>
    <Typography color={'error'}>
      App!!!
    </Typography>
  </div>
}

export default App